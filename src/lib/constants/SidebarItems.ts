interface Items {
  label: string;
  path: string;
}
export const Responsibles: Items[] = [
  { path: '/tareas', label: 'Tareas' },
  { path: '/chat', label: 'Chat' },
];

export default Responsibles;